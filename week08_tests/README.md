# Week 8 — Create Tests J3 + J4

## J3 — Viewport (`test_03_viewport_screenshot.py`)

- Opens `models/sample_box.FCStd` via launcher argument
- Verifies title and model tree (`sample_box`, `Box`)
- Viewport: Fit All + Front view (keyboard shortcuts)
- Screenshot: `artifacts/j3_viewport_sample_box.png`
- Pillow: file size and dimensions

## J4 — Geometry API (`test_04_tolerance_check.py`)

- Creates cube via `Part.makeBox` in **freecadcmd** (no GUI)
- Validates bounding box with `math.isclose` tolerance
- Uses `framework/assertions/geometry_assertions.py`

## Prepared model

`models/sample_box.FCStd` — 10 mm cube, generated by:

```powershell
& "C:\Program Files\FreeCAD 1.1\bin\freecadcmd.exe" scripts/create_sample_box.py
```

## Run tests

```powershell
venv\Scripts\activate
pytest tests/junior/test_03_viewport_screenshot.py -v
pytest tests/junior/test_04_tolerance_check.py -v
```

Expected: 3 + 3 = 6 passed (J3 needs FreeCAD GUI locally).

Progress log: [docs/PROGRESS.md](../docs/PROGRESS.md)
